'use strict';

/**
 * Libraries
 */
var gulp = require('gulp'),
    watch = require('gulp-watch');

/**
 * Watches for asset changes
 */
gulp.task('watch', function () {
});

/**
 * Task groups
 */
gulp.task('default', []);
